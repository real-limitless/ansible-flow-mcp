{
  "mcpServers": {
    "ansible-flow": {
      "command": "uvx",
      "args": ["--from", "ansible-flow-mcp", "ansible-flow-mcp"],
      "env": {
        "ANSIBLE_FLOW_TIMEOUT": "120"
      }
    }
  }
}
