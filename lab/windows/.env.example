# Copy to lab/windows/.env (gitignored) to override lab Windows defaults.
# These are LAB-ONLY credentials for dockur/windows guests.

WIN_USERNAME=Docker
WIN_PASSWORD=admin
WIN_RAM_SIZE=4G
WIN_CPU_CORES=2
WIN_DISK_SIZE=64G
WIN_CLIENT_VERSION=11
WIN_SERVER_VERSION=2022
