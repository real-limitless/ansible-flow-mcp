{
  "fqcn": "infra.quay_configuration.quay_tag",
  "shortDescription": "Manage Quay Container Registry image tags",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/infra/quay_configuration/quay_tag_module.html",
  "options": [
    {
      "name": "expiration",
      "displayName": "Expiration",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Expiration date and time for the tag. The format is C(YYYYMMDDHHMM.SS) but you can change it by setting the O(expiration_format) parameter. You cannot set an expiration date more that two years in the future. If you do so, then Quay forces the date at that two years boundary. You cannot set an expiration date in the past.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "expiration_format",
      "displayName": "Expiration Format",
      "type": "string",
      "required": false,
      "default": "%Y%m%d%H%M.%S",
      "description": "Indicate the time format used in the O(expiration) parameter. Based on default Python format (see U(https://docs.python.org/3/library/time.html#time.strftime)).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "image",
      "displayName": "Image",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Name of the existing image. The format is C(namespace)/C(repository):C(tag) or C(namespace)/C(repository)@C(digest). The namespace can be an organization or a personal namespace. If you omit the namespace part, then the module looks for the repository in your personal namespace. If you omit the tag and the digest part, then V(latest) is assumed.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "immutable",
      "displayName": "Immutable",
      "type": "boolean",
      "required": false,
      "default": null,
      "description": "Whether the tag must be immutable or not. Immutability prevents a tag from being changed or deleted. Requires Quay version 3.17 or later.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_host",
      "displayName": "Quay Host",
      "type": "string",
      "required": false,
      "default": "http://127.0.0.1",
      "description": "URL for accessing the API. U(https://quay.example.com:8443) for example. If you do not set the parameter, then the module uses the E(QUAY_HOST) environment variable. If you do no set the environment variable either, then the module uses the U(http://127.0.0.1) URL.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_password",
      "displayName": "Quay Password",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The password to use for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_PASSWORD) environment variable. If you set O(quay_password), then you also need to set O(quay_username). Mutually exclusive with O(quay_token).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_token",
      "displayName": "Quay Token",
      "type": "string",
      "required": false,
      "default": null,
      "description": "OAuth access token for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_TOKEN) environment variable. Mutually exclusive with O(quay_username) and O(quay_password).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_username",
      "displayName": "Quay Username",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The username to use for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_USERNAME) environment variable. If you set O(quay_username), then you also need to set O(quay_password). Mutually exclusive with O(quay_token).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "If V(absent), then the module deletes the image which tag is given in the O(tag) parameter, or if not set, in the image name. If V(present), then the module adds the tag in the O(tag) parameter to the image.",
      "choices": [
        "absent",
        "present"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "tag",
      "displayName": "Tag",
      "type": "string",
      "required": false,
      "default": null,
      "description": "When O(state=present), the O(tag) parameter provides the new tag to add to the image. If another image already uses that tag, then the module removes the tag from that other image first. When O(state=absent), the O(tag) parameter indicates the tag to remove. If you do not set that O(tag) parameter, then the module removes the tag that you give in the image name with the O(image) parameter. When you specify the image by its digest, in the O(image) parameter, then that O(tag) parameter is mandator",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "timeout",
      "displayName": "Timeout",
      "type": "number",
      "required": false,
      "default": 10.0,
      "description": "Number of seconds to wait for Quay to send data before giving up. If you do not set the parameter, then the module tries the E(QUAY_TIMEOUT) environment variable.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "validate_certs",
      "displayName": "Validate Certs",
      "type": "boolean",
      "required": false,
      "default": true,
      "description": "Whether to allow insecure connections to the API. If V(false), then the module does not validate SSL certificates. If you do not set the parameter, then the module tries the E(QUAY_VERIFY_SSL) environment variable (V(yes), V(1), and V(True) mean true, and V(no), V(0), V(False), and no value mean false).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
