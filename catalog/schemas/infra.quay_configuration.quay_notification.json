{
  "fqcn": "infra.quay_configuration.quay_notification",
  "shortDescription": "Manage Quay Container Registry repository notifications",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/infra/quay_configuration/quay_notification_module.html",
  "options": [
    {
      "name": "config",
      "displayName": "Config",
      "type": "dict",
      "required": false,
      "default": null,
      "description": "Configuration parameters for the notification method.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "event",
      "displayName": "Event",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Event that triggers the notification. Depending of the activated Quay components, not all events might be available on your system.",
      "choices": [
        "repo_push",
        "build_failure",
        "build_queued",
        "build_start",
        "build_success",
        "build_cancelled",
        "vulnerability_found",
        "repo_mirror_sync_started",
        "repo_mirror_sync_success",
        "repo_mirror_sync_failed",
        "repo_image_expiry"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "image_expiry_days",
      "displayName": "Image Expiry Days",
      "type": "number",
      "required": false,
      "default": 7,
      "description": "Only used when O(event) is V(repo_image_expiry). The notification is triggered when the image expires in the specified number of days.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "method",
      "displayName": "Method",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Notification method. Each method requires a specific set of options that you define by using the O(config) parameter. The email notification method is only available on Quay installations where the mailing capability has been activated (C(FEATURE_MAILING) to C(true) in C(config.yaml)).",
      "choices": [
        "email",
        "flowdock",
        "hipchat",
        "quay_notification",
        "slack",
        "webhook"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_host",
      "displayName": "Quay Host",
      "type": "string",
      "required": false,
      "default": "http://127.0.0.1",
      "description": "URL for accessing the API. U(https://quay.example.com:8443) for example. If you do not set the parameter, then the module uses the E(QUAY_HOST) environment variable. If you do no set the environment variable either, then the module uses the U(http://127.0.0.1) URL.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_password",
      "displayName": "Quay Password",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The password to use for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_PASSWORD) environment variable. If you set O(quay_password), then you also need to set O(quay_username). Mutually exclusive with O(quay_token).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_token",
      "displayName": "Quay Token",
      "type": "string",
      "required": false,
      "default": null,
      "description": "OAuth access token for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_TOKEN) environment variable. Mutually exclusive with O(quay_username) and O(quay_password).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_username",
      "displayName": "Quay Username",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The username to use for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_USERNAME) environment variable. If you set O(quay_username), then you also need to set O(quay_password). Mutually exclusive with O(quay_token).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "regexp",
      "displayName": "Regexp",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The regular expression to search in the title of the existing notifications. This does not have to match the entire title. The module uses that regular expression to select the notifications to process. For O(state=present), the module resets the failure counter (if O(reset_failcount) is V(true)) or initiates a test (if O(test) is V(true)) of all the matching notifications. For O(state=absent), the module deletes all the notifications that match. Uses Python regular expressions. See U(https://do",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "repository",
      "displayName": "Repository",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Name of the repository which contains the notifications to manage. The format for the name is C(namespace)/C(shortname). The namespace can be an organization or your personal namespace. If you omit the namespace part in the name, then the module looks for the repository in your personal namespace. You can manage notifications for repositories in your personal namespace, but not in the personal namespace of other users. The token you use in O(quay_token) determines the user account you are using.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "reset_failcount",
      "displayName": "Reset Failcount",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Reset the notification failure counter.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "search_string",
      "displayName": "Search String",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The literal string to search in the title of the existing notifications. This does not have to match the entire line. For O(state=present), the module resets the failure counter (if O(reset_failcount) is V(true)) or initiates a test (if O(test) is V(true)) of all the matching notifications. For O(state=absent), the module deletes all the notifications that match. Mutually exclusive with O(regexp).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "If V(absent), then the module deletes the notification. The module uses the O(title), O(regex), or O(search_string) parameters to select the notifications to process. You can also omit those parameters and use instead the O(event) and O(method) options to select all the notifications triggered by a specific event or using a specific method. If V(present), then the module creates the notification if it does not already exist. If the notification already exists and O(reset_failcount) or O(test) ar",
      "choices": [
        "absent",
        "present"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "test",
      "displayName": "Test",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Initiate a test of the notification.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "timeout",
      "displayName": "Timeout",
      "type": "number",
      "required": false,
      "default": 10.0,
      "description": "Number of seconds to wait for Quay to send data before giving up. If you do not set the parameter, then the module tries the E(QUAY_TIMEOUT) environment variable.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "title",
      "displayName": "Title",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Notification title.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "validate_certs",
      "displayName": "Validate Certs",
      "type": "boolean",
      "required": false,
      "default": true,
      "description": "Whether to allow insecure connections to the API. If V(false), then the module does not validate SSL certificates. If you do not set the parameter, then the module tries the E(QUAY_VERIFY_SSL) environment variable (V(yes), V(1), and V(True) mean true, and V(no), V(0), V(False), and no value mean false).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "vulnerability_level",
      "displayName": "Vulnerability Level",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Only used when O(event) is V(vulnerability_found). The notification is triggered when the vulnerability has a level equal or higher to the level that you define in O(vulnerability_level).",
      "choices": [
        "critical",
        "high",
        "medium",
        "low",
        "negligible",
        "unknown"
      ],
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
