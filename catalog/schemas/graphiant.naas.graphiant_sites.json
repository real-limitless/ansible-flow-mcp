{
  "fqcn": "graphiant.naas.graphiant_sites",
  "shortDescription": "Manage Graphiant sites and object attachments",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/graphiant/naas/graphiant_sites_module.html",
  "options": [
    {
      "name": "access_token",
      "displayName": "Access Token",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Bearer token for API authentication (for example, from C(graphiant login), which opens a browser for sign-in (SSO or non-SSO) and retrieves the token). If not passed as a module argument, the collection reads C(GRAPHIANT_ACCESS_TOKEN) (set after C(graphiant login) when you C(source ~/.graphiant/env.sh)). When a bearer token is present (module argument or environment), it takes precedence over O(username) and O(password). If no valid token is available, the module authenticates with O(username) a",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "detailed_logs",
      "displayName": "Detailed Logs",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Enable detailed logging output for troubleshooting and monitoring. When enabled, provides comprehensive logs of all site operations. Logs are captured and included in the result_msg for display using M(ansible.builtin.debug) module.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "host",
      "displayName": "Host",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Graphiant portal host URL for API connectivity. Example: \"https://api.graphiant.com\"",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "operation",
      "displayName": "Operation",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The specific site operation to perform. V(configure): Create sites and attach global objects in one operation. V(deconfigure): Detach global objects and delete sites in one operation. V(configure_sites): Create sites only (without attaching objects). V(deconfigure_sites): Delete sites only (without detaching objects). V(attach_objects): Attach global objects to existing sites. V(detach_objects): Detach global objects from sites (without deleting sites).",
      "choices": [
        "configure",
        "deconfigure",
        "configure_sites",
        "deconfigure_sites",
        "attach_objects",
        "detach_objects"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "password",
      "displayName": "Password",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Graphiant portal password for authentication. Required for password-based login when no valid bearer token is available from O(access_token) or C(GRAPHIANT_ACCESS_TOKEN).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "site_config_file",
      "displayName": "Site Config File",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Path to the site configuration YAML file. Required for all operations. Can be an absolute path or relative path. Relative paths are resolved using the configured config_path. Configuration files support Jinja2 templating syntax for dynamic generation. File must contain site definitions with site names and global object attachments.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "The desired state of the sites. V(present): Maps to V(configure) when O(operation) not specified. V(absent): Maps to V(deconfigure) when O(operation) not specified.",
      "choices": [
        "present",
        "absent"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "username",
      "displayName": "Username",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Graphiant portal username for authentication. Required for password-based login when no valid bearer token is available from O(access_token) or C(GRAPHIANT_ACCESS_TOKEN).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
