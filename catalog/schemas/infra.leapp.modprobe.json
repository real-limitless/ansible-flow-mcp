{
  "fqcn": "infra.leapp.modprobe",
  "shortDescription": "Load or unload kernel modules",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/infra/leapp/modprobe_module.html",
  "options": [
    {
      "name": "name",
      "displayName": "Name",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Name of kernel module to manage.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "params",
      "displayName": "Params",
      "type": "string",
      "required": false,
      "default": "",
      "description": "Modules parameters.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "persistent",
      "displayName": "Persistent",
      "type": "string",
      "required": false,
      "default": "disabled",
      "description": "Persistency between reboots for configured module. This option creates files in C(/etc/modules-load.d/) and C(/etc/modprobe.d/) that make your module configuration persistent during reboots. If V(present), adds module name to C(/etc/modules-load.d/) and params to C(/etc/modprobe.d/) so the module is loaded on next reboot. If V(absent), comments out module name from C(/etc/modules-load.d/) and comments out params from C(/etc/modprobe.d/) so the module is not loaded on next reboot. If V(disabled),",
      "choices": [
        "disabled",
        "absent",
        "present"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "Whether the module should be present or absent.",
      "choices": [
        "absent",
        "present"
      ],
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
