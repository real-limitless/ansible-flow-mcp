{
  "fqcn": "graphiant.naas.graphiant_nat_policy",
  "shortDescription": "Manage device NAT policy rulesets and LAN segment attachments",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/graphiant/naas/graphiant_nat_policy_module.html",
  "options": [
    {
      "name": "access_token",
      "displayName": "Access Token",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Bearer token for API authentication (for example, from C(graphiant login), which opens a browser for sign-in (SSO or non-SSO) and retrieves the token). If not passed as a module argument, the collection reads C(GRAPHIANT_ACCESS_TOKEN) (set after C(graphiant login) when you C(source ~/.graphiant/env.sh)). When a bearer token is present (module argument or environment), it takes precedence over O(username) and O(password). If no valid token is available, the module authenticates with O(username) a",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "detailed_logs",
      "displayName": "Detailed Logs",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Enable detailed logging.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "device",
      "displayName": "Device",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Portal device hostname for single-device or loop execution. When combined with O(nat_policy_config_file), overrides that device's C(natRulesets) and/or C(segments) in the file with the values supplied via O(natRulesets) / O(segments). Required when O(nat_policy_config_file) is omitted.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "host",
      "displayName": "Host",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Graphiant portal host URL for API connectivity. Example: \"https://api.graphiant.com\"",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "natRulesets",
      "displayName": "Natrulesets",
      "type": "dict",
      "required": false,
      "default": null,
      "description": "NAT ruleset definitions for the device named by O(device) (C(configure) and C(deconfigure) operations). Dict keyed by ruleset name. Each value is either a ruleset body dict (containing C(rules) list/dict and optional C(name)) or C({state: absent}) to delete that ruleset. Within a ruleset body, individual rules in the C(rules) list may include C(state: absent) to delete only that rule. Deleting a ruleset that is still attached to LAN segments raises an error. Detach the segments first (C(detach_f",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "nat_policy_config_file",
      "displayName": "Nat Policy Config File",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Path to the NAT policy YAML file. Can be an absolute path or relative to the configured config_path. Expected top-level key is C(natPolicyObject) (list of devices). Each device may define C(natRulesets) and/or C(segments) in the same file. C(configure)/C(deconfigure) use C(natRulesets); attach/detach operations use C(segments). Optional when O(device) is set with at least O(natRulesets) or O(segments).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "operation",
      "displayName": "Operation",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Specific operation to perform. C(configure) creates/updates rulesets listed under C(natRulesets). Use C(state: absent) on a ruleset entry or an individual rule to delete only that object. Pair with C(attach_to_lan_segments) (or the playbook C(configure) tag) to attach rulesets to LAN segments. C(deconfigure) deletes all listed rulesets by setting C(ruleset: null). A ruleset still attached to LAN segments raises an error \u2014 use C(detach_from_lan_segments) or C(attach_to_lan_segments) with C(state:",
      "choices": [
        "configure",
        "deconfigure",
        "attach_to_lan_segments",
        "detach_from_lan_segments"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "password",
      "displayName": "Password",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Graphiant portal password for authentication. Required for password-based login when no valid bearer token is available from O(access_token) or C(GRAPHIANT_ACCESS_TOKEN).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "segments",
      "displayName": "Segments",
      "type": "dict",
      "required": false,
      "default": null,
      "description": "LAN segment to NAT ruleset mapping for the device named by O(device) (C(attach_to_lan_segments) and C(detach_from_lan_segments) operations). Dict keyed by segment name. For C(attach_to_lan_segments), each value may be one of: a ruleset name string, a dict with C(natRuleset) or C(ruleset) keys, or C({state: absent}) to detach that segment from its current ruleset reference. For C(detach_from_lan_segments), values are not used \u2014 all listed segments are detached. When combined with O(nat_policy_con",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "Desired state for NAT policy rulesets. C(present) maps to C(configure) when C(operation) is omitted. C(absent) maps to C(deconfigure) when C(operation) is omitted.",
      "choices": [
        "present",
        "absent"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "username",
      "displayName": "Username",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Graphiant portal username for authentication. Required for password-based login when no valid bearer token is available from O(access_token) or C(GRAPHIANT_ACCESS_TOKEN).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
