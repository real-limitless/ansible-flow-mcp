{
  "fqcn": "containers.podman.podman_logout",
  "shortDescription": "Log out of a container registry using podman",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/containers/podman/podman_logout_module.html",
  "options": [
    {
      "name": "all",
      "displayName": "All",
      "type": "boolean",
      "required": false,
      "default": null,
      "description": "Remove the cached credentials for all registries in the auth file.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "authfile",
      "displayName": "Authfile",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Path of the authentication file. Default is ``${XDG_RUNTIME_DIR}/containers/auth.json`` You can also override the default path of the authentication file by setting the ``REGISTRY_AUTH_FILE`` environment variable. ``export REGISTRY_AUTH_FILE=path``",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "executable",
      "displayName": "Executable",
      "type": "string",
      "required": false,
      "default": "podman",
      "description": "Path to C(podman) executable if it is not in the C($PATH) on the machine running C(podman)",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "ignore_docker_credentials",
      "displayName": "Ignore Docker Credentials",
      "type": "boolean",
      "required": false,
      "default": null,
      "description": "Credentials created using other tools such as `docker login` are not removed unless the corresponding `authfile` is explicitly specified. Since podman also uses existing credentials in these files by default (for docker e.g. `${HOME}/.docker/config.json`), module execution will fail if a docker login exists for the registry specified in any `authfile` is used by podman. This can be ignored by setting `ignore_docker_credentials` to `true` - the credentials will be kept and `changed` will be false",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "registry",
      "displayName": "Registry",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Registry server. If the registry is not specified, the first registry under `[registries.search]` from `registries.conf` will be used.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
