{
  "fqcn": "infra.quay_configuration.quay_application",
  "shortDescription": "Manage Quay Container Registry applications",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/infra/quay_configuration/quay_application_module.html",
  "options": [
    {
      "name": "application_uri",
      "displayName": "Application Uri",
      "type": "string",
      "required": false,
      "default": null,
      "description": "URL to the application home page.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "avatar_email",
      "displayName": "Avatar Email",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Email address that represents the avatar for the application.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "description",
      "displayName": "Description",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Description for the application.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "name",
      "displayName": "Name",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Name of the application to create, update, or delete. Application names must be at least two characters long.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "new_name",
      "displayName": "New Name",
      "type": "string",
      "required": false,
      "default": null,
      "description": "New name for the application. Setting this option changes the name of the application which current name is provided in O(name).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "organization",
      "displayName": "Organization",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Name of the organization in which to manage the application.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_host",
      "displayName": "Quay Host",
      "type": "string",
      "required": false,
      "default": "http://127.0.0.1",
      "description": "URL for accessing the API. U(https://quay.example.com:8443) for example. If you do not set the parameter, then the module uses the E(QUAY_HOST) environment variable. If you do no set the environment variable either, then the module uses the U(http://127.0.0.1) URL.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_password",
      "displayName": "Quay Password",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The password to use for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_PASSWORD) environment variable. If you set O(quay_password), then you also need to set O(quay_username). Mutually exclusive with O(quay_token).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_token",
      "displayName": "Quay Token",
      "type": "string",
      "required": false,
      "default": null,
      "description": "OAuth access token for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_TOKEN) environment variable. Mutually exclusive with O(quay_username) and O(quay_password).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_username",
      "displayName": "Quay Username",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The username to use for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_USERNAME) environment variable. If you set O(quay_username), then you also need to set O(quay_password). Mutually exclusive with O(quay_token).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "redirect_uri",
      "displayName": "Redirect Uri",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Prefix of the application's OAuth redirection/callback URLs.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "If V(absent), then the module deletes the application. The module does not fail if the application does not exist, because the state is already as expected. If V(present), then the module creates the application if it does not already exist. If the application already exists, then the module updates its state.",
      "choices": [
        "absent",
        "present"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "timeout",
      "displayName": "Timeout",
      "type": "number",
      "required": false,
      "default": 10.0,
      "description": "Number of seconds to wait for Quay to send data before giving up. If you do not set the parameter, then the module tries the E(QUAY_TIMEOUT) environment variable.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "validate_certs",
      "displayName": "Validate Certs",
      "type": "boolean",
      "required": false,
      "default": true,
      "description": "Whether to allow insecure connections to the API. If V(false), then the module does not validate SSL certificates. If you do not set the parameter, then the module tries the E(QUAY_VERIFY_SSL) environment variable (V(yes), V(1), and V(True) mean true, and V(no), V(0), V(False), and no value mean false).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
