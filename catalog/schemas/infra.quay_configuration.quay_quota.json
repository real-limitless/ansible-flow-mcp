{
  "fqcn": "infra.quay_configuration.quay_quota",
  "shortDescription": "Manage Quay Container Registry organizations quota",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/infra/quay_configuration/quay_quota_module.html",
  "options": [
    {
      "name": "organization",
      "displayName": "Organization",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Name of the organization. This organization must exist.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_host",
      "displayName": "Quay Host",
      "type": "string",
      "required": false,
      "default": "http://127.0.0.1",
      "description": "URL for accessing the API. U(https://quay.example.com:8443) for example. If you do not set the parameter, then the module uses the E(QUAY_HOST) environment variable. If you do no set the environment variable either, then the module uses the U(http://127.0.0.1) URL.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_password",
      "displayName": "Quay Password",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The password to use for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_PASSWORD) environment variable. If you set O(quay_password), then you also need to set O(quay_username). Mutually exclusive with O(quay_token).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_token",
      "displayName": "Quay Token",
      "type": "string",
      "required": false,
      "default": null,
      "description": "OAuth access token for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_TOKEN) environment variable. Mutually exclusive with O(quay_username) and O(quay_password).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_username",
      "displayName": "Quay Username",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The username to use for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_USERNAME) environment variable. If you set O(quay_username), then you also need to set O(quay_password). Mutually exclusive with O(quay_token).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quota",
      "displayName": "Quota",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Quota that Quay uses to compute the warning and reject limits for the organization. You specify a quota in bytes, but you can also use the K[i]B, M[i]B, G[i]B, or T[i]B suffixes.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "reject_pct",
      "displayName": "Reject Pct",
      "type": "number",
      "required": false,
      "default": null,
      "description": "Reject (hard) limit as a percentage of the quota. Quay terminates any image push in the organization when the limit is reached. Set O(reject_pct) to V(0) to remove the reject limit.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "If V(absent), then the module deletes the quota and limits for the given organization. If V(present), then the module establishes the quota and limits for the given organization. If quota and limits are already set, then the module updates them.",
      "choices": [
        "absent",
        "present"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "timeout",
      "displayName": "Timeout",
      "type": "number",
      "required": false,
      "default": 10.0,
      "description": "Number of seconds to wait for Quay to send data before giving up. If you do not set the parameter, then the module tries the E(QUAY_TIMEOUT) environment variable.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "validate_certs",
      "displayName": "Validate Certs",
      "type": "boolean",
      "required": false,
      "default": true,
      "description": "Whether to allow insecure connections to the API. If V(false), then the module does not validate SSL certificates. If you do not set the parameter, then the module tries the E(QUAY_VERIFY_SSL) environment variable (V(yes), V(1), and V(True) mean true, and V(no), V(0), V(False), and no value mean false).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "warning_pct",
      "displayName": "Warning Pct",
      "type": "number",
      "required": false,
      "default": null,
      "description": "Warning (soft) limit as a percentage of the quota. Quay notifies the users when the limit is reached. Set O(warning_pct) to V(0) to remove the warning limit.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
