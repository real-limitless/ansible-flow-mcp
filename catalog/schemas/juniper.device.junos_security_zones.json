{
  "fqcn": "juniper.device.junos_security_zones",
  "shortDescription": "Manage security zones on Juniper JUNOS devices",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_security_zones_module.html",
  "options": [
    {
      "name": "comment",
      "displayName": "Comment",
      "type": "string",
      "required": false,
      "default": "configured by junos_security_zones",
      "description": "Commit comment to be associated with the configuration changes. This allows tracking the reason for configuration changes (e.g., change management ticket).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "config",
      "displayName": "Config",
      "type": "dict",
      "required": false,
      "default": null,
      "description": "Dictionary of security zone parameters",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "running_config",
      "displayName": "Running Config",
      "type": "string",
      "required": false,
      "default": null,
      "description": "This option is used only with state I(parsed). The value of this option should be the output received from the JunOS device by executing the command B(show security policies). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "merged",
      "description": "The state the configuration should be left in The states I(rendered), I(gathered) and I(parsed) does not perform any change on the device. The state I(rendered) will transform the configuration in C(config) option to platform specific CLI commands which will be returned in the I(rendered) key within the result. For state I(rendered) active connection to remote host is not required. behaviour for this module. The state I(gathered) will fetch the running configuration from device and transform it ",
      "choices": [
        "merged",
        "replaced",
        "overridden",
        "deleted",
        "rendered",
        "gathered",
        "parsed"
      ],
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
