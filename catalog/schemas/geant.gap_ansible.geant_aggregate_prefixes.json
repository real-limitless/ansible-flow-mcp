{
  "fqcn": "geant.gap_ansible.geant_aggregate_prefixes",
  "shortDescription": "Aggregate IP prefixes into the minimal set of CIDR blocks",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/geant/gap_ansible/geant_aggregate_prefixes_module.html",
  "options": [
    {
      "name": "prefixes",
      "displayName": "Prefixes",
      "type": "list",
      "required": true,
      "default": null,
      "description": "A list of IP prefixes to be aggregated. Each prefix should be a valid CIDR block (e.g., 192.0.2.0/32).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
