{
  "fqcn": "f5networks.f5os.velos_partition_ha_config",
  "shortDescription": "Managing high availability for the VELOS partition",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/f5networks/f5os/velos_partition_ha_config_module.html",
  "options": [
    {
      "name": "auto_failback",
      "displayName": "Auto Failback",
      "type": "dict",
      "required": false,
      "default": null,
      "description": "Dictionary to configure auto-failback behavior for the partition. When B(enabled), the system will automatically fail back to the preferred controller after a failover event. If the partition C(prefer_mode) remains as C(auto), you do not have to B(enable) the C(auto_failback). The C(failback_delay) specifies the number of seconds to wait before failback occurs.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "prefer_node",
      "displayName": "Prefer Node",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Specifies which controller mode should be active. When set to C(prefer-1), controller 1 will be set as active. When set to C(prefer-2), controller 2 will be set as active. When set to C(active-controller), the active controller will be set based on the current active controller. When set to C(auto), the system will automatically determine the active controller.",
      "choices": [
        "prefer-1",
        "prefer-2",
        "active-controller",
        "auto"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "When C(present), ensures the specified controller is active. When C(absent), This module does not support removing the active controller setting.",
      "choices": [
        "present"
      ],
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
