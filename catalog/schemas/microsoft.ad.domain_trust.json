{
  "fqcn": "microsoft.ad.domain_trust",
  "shortDescription": "Manage Active Directory domain trusts",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/microsoft/ad/domain_trust_module.html",
  "options": [
    {
      "name": "direction",
      "displayName": "Direction",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The direction of the trust relationship. V(inbound) allows users in the target domain to authenticate in the local domain. V(outbound) allows users in the local domain to authenticate in the target domain. V(bidirectional) allows authentication in both directions. Required when O(state=present) and the trust does not yet exist. Cannot be changed on an existing trust; remove and recreate the trust to change direction.",
      "choices": [
        "inbound",
        "outbound",
        "bidirectional"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "domain_server",
      "displayName": "Domain Server",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The FQDN of the domain controller to target for all AD operations. When not specified the module uses default domain controller discovery.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "name",
      "displayName": "Name",
      "type": "string",
      "required": true,
      "default": null,
      "description": "The fully qualified domain name (FQDN) of the target domain to trust (e.g. V(example.com)). This is the primary key used to identify the trust.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "selective_authentication",
      "displayName": "Selective Authentication",
      "type": "boolean",
      "required": false,
      "default": null,
      "description": "Whether selective authentication is enabled on the trust. When V(true), users in the trusted domain are not automatically authenticated. Permissions must be granted explicitly on each resource. This is the only property that can be updated on an existing trust without removing it first.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "Whether the trust should be present or absent.",
      "choices": [
        "present",
        "absent"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "trust_password",
      "displayName": "Trust Password",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The shared secret used to establish the trust. Both sides of the trust must use the same password. Required when O(state=present) and the trust does not yet exist.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "type",
      "displayName": "Type",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The type of trust relationship to create. V(forest) creates a transitive trust between two AD forests. V(external) creates a non-transitive trust between two domains that may be in different forests. Required when O(state=present) and the trust does not yet exist. Cannot be changed on an existing trust; remove and recreate the trust to change type.",
      "choices": [
        "external",
        "forest"
      ],
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
