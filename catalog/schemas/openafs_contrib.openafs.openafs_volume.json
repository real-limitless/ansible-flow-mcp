{
  "fqcn": "openafs_contrib.openafs.openafs_volume",
  "shortDescription": "Create an OpenAFS volume",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/openafs_contrib/openafs/openafs_volume_module.html",
  "options": [
    {
      "name": "acl",
      "displayName": "Acl",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The access control list to be set in the volumes root directory. The C(acl) option my be specified as a list of strings. Each string contains a pair of strings separated by a space. The substring names a user or group, the second indicates the access rights. See C(fs setacl) for details. This option may only be used if a client is installed on the remote node.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "auth_keytab",
      "displayName": "Auth Keytab",
      "type": "string",
      "required": false,
      "default": "admin.keytab",
      "description": "The path on the remote host to the keytab file to be used to authenticate. The keytab file must already be present on the remote host. This option may only be used if a client is installed on the remote node.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "auth_user",
      "displayName": "Auth User",
      "type": "string",
      "required": false,
      "default": "admin",
      "description": "The afs user name to be used when C(localauth) is False. The user must be a member of the C(system:administrators) group and must be a server superuser, that is, set in the C(UserList) file on each server in the cell. Old kerberos 4 '.' separators are automatically converted to modern '/' separators. This option may only be used if a client is installed on the remote node.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "localauth",
      "displayName": "Localauth",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Indicates if the C(-localauth) option is to be used for authentication. This option should only be used when running on a server. The C(mount) and C(acl) options may not be used with C(localauth).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "mount",
      "displayName": "Mount",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The initial mount point path. Should be the fully-qualified path to the mount point to be created. The read/write path variant will be used if it is available. A read/write mount point will also be created for the C(root.cell) volume. The C(i) and C(a) ACL rights will be temporarily assigned to the mount point parent directory in order to create the mount point if those rights are missing. The volume containing the parent volume will be released if a mount point was created. The volume will be c",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "partition",
      "displayName": "Partition",
      "type": "string",
      "required": false,
      "default": "the first partition found on the fileserver",
      "description": "The initial volume partition id. If provided, should be the partition id; C('a') ..  C('iu'). If not provided, the first partition found from C(vos listpart) will be used. The volume will not be moved if it already exists on a different partition. This option is ignored when the state is C(absent).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quota",
      "displayName": "Quota",
      "type": "number",
      "required": false,
      "default": 0,
      "description": "The initial volume quota.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "replicas",
      "displayName": "Replicas",
      "type": "number",
      "required": false,
      "default": 0,
      "description": "The number of read-only volumes to be created, including the read-only clone on the same fileserver and partition as the read/write volume. The C(replicas) option indicates the minumum number of read-only volumes desired.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "server",
      "displayName": "Server",
      "type": "string",
      "required": false,
      "default": "first fileserver entry found in VLDB",
      "description": "The initial volume fileserver location. If provided, should be the hostname or fileserver address. If not provided, the first fileserver address from C(vos listaddrs) will be used. The volume will not be moved if it already exists on a different server. This option is ignored when the state is C(absent).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "<present>",
      "description": "C(present) ensure the volume is present, C(absent) ensure the volume is removed",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "volume",
      "displayName": "Volume",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Name of the read-write volume.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
