{
  "fqcn": "community.general.crypttab",
  "shortDescription": "Encrypted Linux block devices",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/community/general/crypttab_module.html",
  "options": [
    {
      "name": "backing_device",
      "displayName": "Backing Device",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Path to the underlying block device or file, or the UUID of a block-device prefixed with V(UUID=).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "name",
      "displayName": "Name",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Name of the encrypted block device as it appears in the C(/etc/crypttab) file, or optionally prefixed with V(/dev/mapper/), as it appears in the filesystem. V(/dev/mapper/) is stripped from O(name).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "opts",
      "displayName": "Opts",
      "type": "string",
      "required": false,
      "default": null,
      "description": "A comma-delimited list of options. See V(crypttab(5\\)) for details.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "password",
      "displayName": "Password",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Encryption password, the path to a file containing the password, or V(-) or unset if the password should be entered at boot.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "path",
      "displayName": "Path",
      "type": "string",
      "required": false,
      "default": "/etc/crypttab",
      "description": "Path to file to use instead of V(/etc/crypttab). This might be useful in a chroot environment.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Use V(present) to add a line to C(/etc/crypttab) or update its definition if already present. Use V(absent) to remove a line with matching O(name). Use V(opts_present) to add options to those already present; options with different values are updated. Use V(opts_absent) to remove options from the existing set.",
      "choices": [
        "absent",
        "opts_absent",
        "opts_present",
        "present"
      ],
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
