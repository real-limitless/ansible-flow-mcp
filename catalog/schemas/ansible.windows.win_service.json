{
  "fqcn": "ansible.windows.win_service",
  "shortDescription": "Manage Windows services",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/ansible/windows/win_service_module.html",
  "options": [
    {
      "name": "dependencies",
      "displayName": "Dependencies",
      "type": "list",
      "required": false,
      "default": null,
      "description": "A list of service dependencies to set for this particular service. This should be a list of service names and not the display name of the service. This works by C(dependency_action) to either add/remove or set the services in this list.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "dependency_action",
      "displayName": "Dependency Action",
      "type": "string",
      "required": false,
      "default": "set",
      "description": "Used in conjunction with C(dependency) to either add the dependencies to the existing service dependencies. Remove the dependencies to the existing dependencies. Set the dependencies to only the values in the list replacing the existing dependencies.",
      "choices": [
        "add",
        "remove",
        "set"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "description",
      "displayName": "Description",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The description to set for the service.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "desktop_interact",
      "displayName": "Desktop Interact",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Whether to allow the service user to interact with the desktop. This can only be set to C(true) when using the C(LocalSystem) username. This can only be set to C(true) when the I(service_type) is C(win32_own_process) or C(win32_share_process).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "display_name",
      "displayName": "Display Name",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The display name to set for the service.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "error_control",
      "displayName": "Error Control",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The severity of the error and action token if the service fails to start. A new service defaults to C(normal). C(critical) will log the error and restart the system with the last-known good configuration. If the startup fails on reboot then the system will fail to operate. C(ignore) ignores the error. C(normal) logs the error in the event log but continues. C(severe) is like C(critical) but a failure on the last-known good configuration reboot startup will be ignored.",
      "choices": [
        "critical",
        "ignore",
        "normal",
        "severe"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "failure_actions",
      "displayName": "Failure Actions",
      "type": "list",
      "required": false,
      "default": null,
      "description": "A list of failure actions the service controller should take on each failure of a service. The service manager will run the actions from first to last defined until the service starts. If I(failure_reset_period_sec) has been exceeded then the failure actions will restart from the beginning. If all actions have been performed the the service manager will repeat the last service defined. The existing actions will be replaced with the list defined in the task if there is a mismatch with any of them",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "failure_actions_on_non_crash_failure",
      "displayName": "Failure Actions On Non Crash Failure",
      "type": "boolean",
      "required": false,
      "default": null,
      "description": "Controls whether failure actions will be performed on non crash failures or not.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "failure_command",
      "displayName": "Failure Command",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The command to run for a C(run_command) failure action. Set to an empty string to remove the command.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "failure_reboot_msg",
      "displayName": "Failure Reboot Msg",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The message to be broadcast to users logged on the host for a C(reboot) failure action. Set to an empty string to remove the message.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "failure_reset_period_sec",
      "displayName": "Failure Reset Period Sec",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The time in seconds after which the failure action list resets back to the start of the list if there are no failures. To set this value, I(failure_actions) must have at least 1 action present. Specify C('0xFFFFFFFF') to set an infinite reset period.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "force_dependent_services",
      "displayName": "Force Dependent Services",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "If C(true), stopping or restarting a service with dependent services will force the dependent services to stop or restart also. If C(false), stopping or restarting a service with dependent services may fail.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "load_order_group",
      "displayName": "Load Order Group",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The name of the load ordering group of which this service is a member. Specify an empty string to remove the existing load order group of a service.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "name",
      "displayName": "Name",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Name of the service. If only the name parameter is specified, the module will report on whether the service exists or not without making any changes.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "password",
      "displayName": "Password",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The password to set the service to start as. This and the C(username) argument should be supplied together when using a local or domain account. If omitted then the password will continue to use the existing value password set. If specifying C(LocalSystem), C(NetworkService), C(LocalService), the C(NT SERVICE), or a gMSA this field can be omitted as those accounts have no password.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "path",
      "displayName": "Path",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The path to the executable to set for the service.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "pre_shutdown_timeout_ms",
      "displayName": "Pre Shutdown Timeout Ms",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The time in which the service manager waits after sending a preshutdown notification to the service until it proceeds to continue with the other shutdown actions.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "required_privileges",
      "displayName": "Required Privileges",
      "type": "list",
      "required": false,
      "default": null,
      "description": "A list of privileges the service must have when starting up. When set the service will only have the privileges specified on its access token. The I(username) of the service must already have the privileges assigned. The existing privileges will be replace with the list defined in the task if there is a mismatch with any of them. Set to an empty list to remove all required privileges, otherwise an omitted or null value will keep the existing privileges. See L(privilege text constants,https://doc",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "service_type",
      "displayName": "Service Type",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The type of service. The default type of a new service is C(win32_own_process). I(desktop_interact) can only be set if the service type is C(win32_own_process) or C(win32_share_process).",
      "choices": [
        "user_own_process",
        "user_share_process",
        "win32_own_process",
        "win32_share_process"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "sid_info",
      "displayName": "Sid Info",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Used to define the behaviour of the service's access token groups. C(none) will not add any groups to the token. C(restricted) will add the C(NT SERVICE\\<service name>) SID to the access token's groups and restricted groups. C(unrestricted) will add the C(NT SERVICE\\<service name>) SID to the access token's groups.",
      "choices": [
        "none",
        "restricted",
        "unrestricted"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "start_mode",
      "displayName": "Start Mode",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Set the startup type for the service. A newly created service will default to C(auto).",
      "choices": [
        "auto",
        "delayed",
        "disabled",
        "manual"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The desired state of the service. C(started)/C(stopped)/C(absent)/C(paused) are idempotent actions that will not run commands unless necessary. C(restarted) will always bounce the service. Only services that support the paused state can be paused, you can check the return value C(can_pause_and_continue). You can only pause a service that is already started. A newly created service will default to C(stopped).",
      "choices": [
        "absent",
        "paused",
        "started",
        "stopped",
        "restarted"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "update_password",
      "displayName": "Update Password",
      "type": "string",
      "required": false,
      "default": null,
      "description": "When set to C(always) and I(password) is set, the module will always report a change and set the password. Set to C(on_create) to only set the password if the module needs to create the service. If I(username) was specified and the service changed to that username then I(password) will also be changed if specified. The current default is C(on_create) but this behaviour may change in the future, it is best to be explicit here.",
      "choices": [
        "always",
        "on_create"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "username",
      "displayName": "Username",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The username to set the service to start as. Can also be set to C(LocalSystem) or C(SYSTEM) to use the SYSTEM account. A newly created service will default to C(LocalSystem). If using a custom user account, it must have the C(SeServiceLogonRight) granted to be able to start up. You can use the M(ansible.windows.win_user_right) module to grant this user right for you. Set to C(NT SERVICE\\service name) to run as the NT SERVICE account for that service. This can also be a gMSA in the form C(DOMAIN\\",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
