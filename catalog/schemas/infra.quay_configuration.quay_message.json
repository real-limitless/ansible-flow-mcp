{
  "fqcn": "infra.quay_configuration.quay_message",
  "shortDescription": "Manage Quay Container Registry global messages",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/infra/quay_configuration/quay_message_module.html",
  "options": [
    {
      "name": "content",
      "displayName": "Content",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Text of the message to display on each web UI page.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "format",
      "displayName": "Format",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Format of the text in O(content). If you do not set this parameter, then the module uses the V(plain) format.",
      "choices": [
        "markdown",
        "plain"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_host",
      "displayName": "Quay Host",
      "type": "string",
      "required": false,
      "default": "http://127.0.0.1",
      "description": "URL for accessing the API. U(https://quay.example.com:8443) for example. If you do not set the parameter, then the module uses the E(QUAY_HOST) environment variable. If you do no set the environment variable either, then the module uses the U(http://127.0.0.1) URL.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_password",
      "displayName": "Quay Password",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The password to use for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_PASSWORD) environment variable. If you set O(quay_password), then you also need to set O(quay_username). Mutually exclusive with O(quay_token).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_token",
      "displayName": "Quay Token",
      "type": "string",
      "required": false,
      "default": null,
      "description": "OAuth access token for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_TOKEN) environment variable. Mutually exclusive with O(quay_username) and O(quay_password).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "quay_username",
      "displayName": "Quay Username",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The username to use for authenticating against the API. If you do not set the parameter, then the module tries the E(QUAY_USERNAME) environment variable. If you set O(quay_username), then you also need to set O(quay_password). Mutually exclusive with O(quay_token).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "regexp",
      "displayName": "Regexp",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The regular expression to look for in the existing messages. This does not have to match an entire line. For O(state=present), if several messages match, then the module updates one and deletes the others. For O(state=absent), the module deletes all the messages that match. Uses Python regular expressions. See U(https://docs.python.org/3/library/re.html). Mutually exclusive with O(search_string).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "search_severity",
      "displayName": "Search Severity",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Search messages by their severity level. If you also set O(search_string), O(regexp), or O(content), messages must match all those criteria.",
      "choices": [
        "info",
        "warning",
        "error"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "search_string",
      "displayName": "Search String",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The literal string to look for in the existing messages. This does not have to match an entire line. For O(state=present), if several messages match, then the module updates one and deletes the others. For O(state=absent), the module deletes all the messages that match. Mutually exclusive with O(regexp).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "severity",
      "displayName": "Severity",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Severity of the message. If you do not set this parameter, then the module creates the message with the V(info) severity.",
      "choices": [
        "info",
        "warning",
        "error"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "If V(absent), then the module deletes all the messages which content matches O(search_string), O(regexp), O(content), or O(search_severity). If V(present), then the module creates the message if it does not already exist (that is, if no message matches O(search_string), O(regexp), or O(content)). Is several messages match, only one is updated and the others are deleted.",
      "choices": [
        "absent",
        "present"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "timeout",
      "displayName": "Timeout",
      "type": "number",
      "required": false,
      "default": 10.0,
      "description": "Number of seconds to wait for Quay to send data before giving up. If you do not set the parameter, then the module tries the E(QUAY_TIMEOUT) environment variable.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "validate_certs",
      "displayName": "Validate Certs",
      "type": "boolean",
      "required": false,
      "default": true,
      "description": "Whether to allow insecure connections to the API. If V(false), then the module does not validate SSL certificates. If you do not set the parameter, then the module tries the E(QUAY_VERIFY_SSL) environment variable (V(yes), V(1), and V(True) mean true, and V(no), V(0), V(False), and no value mean false).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
