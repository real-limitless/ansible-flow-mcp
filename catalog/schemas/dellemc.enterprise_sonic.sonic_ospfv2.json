{
  "fqcn": "dellemc.enterprise_sonic.sonic_ospfv2",
  "shortDescription": "Configure global OSPFv2 protocol settings on SONiC.",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/dellemc/enterprise_sonic/sonic_ospfv2_module.html",
  "options": [
    {
      "name": "config",
      "displayName": "Config",
      "type": "list",
      "required": false,
      "default": null,
      "description": "Specifies the OSPFv2 related configuration. I(non_passive_interfaces) and I(passive_interfaces) are mutually exclusive. When I(default_passive=True), I(passive_interfaces) cannot be configured. When I(default_passive=False), I(non_passive_interfaces) cannot be configured.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "merged",
      "description": "Specifies the operation to be performed on the OSPFv2 process configured on the device. In case of merged, the input configuration will be merged with the existing OSPFv2 configuration on the device. In case of deleted, the existing OSPFv2 configuration will be removed from the device. In case of overridden, all the existing OSPFv2 configuration will be deleted and the specified input configuration will be installed. In case of replaced, the existing OSPFv2 configuration on the device will be re",
      "choices": [
        "merged",
        "deleted",
        "replaced",
        "overridden"
      ],
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
