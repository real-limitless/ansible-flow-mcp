{
  "fqcn": "ansible.builtin.apt_repository",
  "shortDescription": "Add and remove APT repositories",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/ansible/builtin/apt_repository_module.html",
  "options": [
    {
      "name": "codename",
      "displayName": "Codename",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Override the distribution codename to use for PPA repositories. Should usually only be set when working with a PPA on a non-Ubuntu target (for example, Debian or Mint).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "filename",
      "displayName": "Filename",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Sets the name of the source list file in C(sources.list.d). Defaults to a file name based on the repository source url. The C(.list) extension will be automatically added.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "install_python_apt",
      "displayName": "Install Python Apt",
      "type": "boolean",
      "required": false,
      "default": true,
      "description": "Whether to automatically try to install the Python apt library or not, if it is not already installed. Without this library, the module does not work. Runs C(apt-get install python-apt) for Python 2, and C(apt-get install python3-apt) for Python 3. Only works with the system Python 2 or Python 3. If you are using a Python on the remote that is not the system Python, set O(install_python_apt=false) and ensure that the Python apt library for your Python version is installed some other way.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "mode",
      "displayName": "Mode",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The octal mode for newly created files in C(sources.list.d). Default is what system uses (probably 0644).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "repo",
      "displayName": "Repo",
      "type": "string",
      "required": true,
      "default": null,
      "description": "A source string for the repository.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "A source string state.",
      "choices": [
        "absent",
        "present"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "update_cache",
      "displayName": "Update Cache",
      "type": "boolean",
      "required": false,
      "default": "yes",
      "description": "Run the equivalent of C(apt-get update) when a change occurs. Cache updates are run after making changes.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "update_cache_retries",
      "displayName": "Update Cache Retries",
      "type": "number",
      "required": false,
      "default": 5,
      "description": "Amount of retries if the cache update fails. Also see O(update_cache_retry_max_delay).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "update_cache_retry_max_delay",
      "displayName": "Update Cache Retry Max Delay",
      "type": "number",
      "required": false,
      "default": 12,
      "description": "Use an exponential backoff delay for each retry (see O(update_cache_retries)) up to this max delay in seconds.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "validate_certs",
      "displayName": "Validate Certs",
      "type": "boolean",
      "required": false,
      "default": "yes",
      "description": "If V(false), SSL certificates for the target repo will not be validated. This should only be used on personally controlled sites using self-signed certificates.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
