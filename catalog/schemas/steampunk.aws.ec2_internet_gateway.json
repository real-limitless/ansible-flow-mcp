{
  "fqcn": "steampunk.aws.ec2_internet_gateway",
  "shortDescription": "Manage EC2 Internet Gateways",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/steampunk/aws/ec2_internet_gateway_module.html",
  "options": [
    {
      "name": "auth",
      "displayName": "Auth",
      "type": "dict",
      "required": false,
      "default": null,
      "description": "Parameters for authenticating with the AWS service. Each of them may be defined via environment variables.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "clear_tags",
      "displayName": "Clear Tags",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Whether to clear any existing tags on the resource that are not explicitly stated in I(tags). By default, existing tags are kept on the resource. When this parameter is set to C(true), any pre-existing tags on the resource (including the name tag) are removed. To clear all tags except the name tag, make sure to provide the I(name) parameter.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "id",
      "displayName": "Id",
      "type": "string",
      "required": false,
      "default": null,
      "description": "ID of the resource to perform the task on. If specified, this parameter is used to identify the resource. Omit this parameter when you are first creating the resource.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "name",
      "displayName": "Name",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Name tag for the internet gateway. This parameter is required when creating the internet gateway. In the absence of I(id), the value of this parameter will be used to try to identify the internet gateway.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "attached",
      "description": "The desired state of the internet gateway. To create and attach an internet gateway to a VPC, or to attach an existing internet gateway to a VPC, use I(state) = C(attached). To create an internet gateway without attaching it to a VPC, or to detach an existing internet gateway from a VPC, use I(state) = C(detached). To remove the internet gateway, use C(absent). Note that if necessary, VPC will be detached prior to removing the internet gateway.",
      "choices": [
        "attached",
        "detached",
        "absent"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "tags",
      "displayName": "Tags",
      "type": "dict",
      "required": false,
      "default": null,
      "description": "Metadata for the AWS resource as key/value pairs. Keys and values are case-sensitive.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "vpc",
      "displayName": "Vpc",
      "type": "string",
      "required": false,
      "default": null,
      "description": "ID of the VPC to attach the internet gateway to. If the internet gateway is already attached to a VPC different to the one specified, the module will detach the gateway from the VPC and attach it to the specified one. Required if I(state) is C(attached). When I(state) is C(detached) or C(absent), the value of this parameter is ignored.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
