{
  "fqcn": "arista.avd.validate_inputs",
  "shortDescription": "Validate variables against AVD schemas",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/arista/avd/validate_inputs_module.html",
  "options": [
    {
      "name": "batch_size",
      "displayName": "Batch Size",
      "type": "number",
      "required": false,
      "default": 10,
      "description": "The number of devices to process per child process during the templating phase.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "device_list",
      "displayName": "Device List",
      "type": "list",
      "required": false,
      "default": null,
      "description": "Optional list of hostnames to process. If not provided, fallback to all hosts in the current play. For `eos_config` and `cv_deploy` schemas, only these hosts will be validated. For `avd_design`, these hosts must be a subset of the `fabric_name` group and the entire group will be processed.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "fail_on_missing_input_files",
      "displayName": "Fail On Missing Input Files",
      "type": "boolean",
      "required": false,
      "default": true,
      "description": "If `true`, the task will fail if any device input files are missing. If `false`, devices with missing input files will be skipped with an informational log message.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "fail_on_validation_errors",
      "displayName": "Fail On Validation Errors",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "If `true`, the task will fail if any validation errors are detected. If `false`, errors will be reported but the task will succeed.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "input_dir",
      "displayName": "Input Dir",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Optional path to a directory containing input files to validate directly. If provided, the templating phase is skipped and files are read from this directory. Files must be named `device_name.input_suffix`.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "input_suffix",
      "displayName": "Input Suffix",
      "type": "string",
      "required": false,
      "default": "json",
      "description": "File suffix for files located in `input_dir`. Only used when `input_dir` is provided.",
      "choices": [
        "yml",
        "yaml",
        "json"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "read_from_input_dir",
      "displayName": "Read From Input Dir",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "If `true`, the templating phase is skipped and input files are read directly from `input_dir`. If `false`, the plugin resolves Ansible hostvars and writes templated data before validation. Requires `input_dir` to be set when `true`.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "schema_name",
      "displayName": "Schema Name",
      "type": "string",
      "required": false,
      "default": "avd_design",
      "description": "The AVD schema to validate against. If set to `avd_design`, the plugin will validate the inputs for the entire fabric (requiring `fabric_name` to be set). If set to `eos_config` or `cv_deploy`, the plugin will validate the inputs for the devices in the `device_list` if provided, otherwise all hosts in the current play.",
      "choices": [
        "avd_design",
        "eos_config",
        "cv_deploy"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "tmp_dir",
      "displayName": "Tmp Dir",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Path to use as the AVD temporary directory for storing templated and validated data used internally by plugins. Must be the same across all plugins.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "validation_configuration",
      "displayName": "Validation Configuration",
      "type": "dict",
      "required": false,
      "default": null,
      "description": "Optional dictionary containing configuration options to control validation behavior.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "vault_id",
      "displayName": "Vault Id",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Vault ID used for encrypting temporary files generated by the plugin.\nWhen Ansible Vault is not configured, this parameter has no effect and files are written as plain JSON.\nWhen Ansible Vault is configured, AVD encrypts files containing templated and validated data\nto prevent sensitive information from being exposed in the temporary directories.\n  * When `vault_id` is not specified, AVD uses the *first* Vault ID in the list for encryption.\n  * When `vault_id` is specified, AVD uses the specifie",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
