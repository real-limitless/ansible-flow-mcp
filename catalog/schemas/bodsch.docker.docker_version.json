{
  "fqcn": "bodsch.docker.docker_version",
  "shortDescription": "Retrieve the current Docker Engine and API version.",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/bodsch/docker/docker_version_module.html",
  "options": [
    {
      "name": "docker_socket",
      "displayName": "Docker Socket",
      "type": "string",
      "required": false,
      "default": "/run/docker.sock",
      "description": "Path to the Docker socket file used to connect to the Docker daemon. If not specified, the module will attempt to use environment-based configuration.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "Desired state of the module. C(present) or C(test) are used for information gathering, while C(absent) is not applicable here.",
      "choices": [
        "absent",
        "present",
        "test"
      ],
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
