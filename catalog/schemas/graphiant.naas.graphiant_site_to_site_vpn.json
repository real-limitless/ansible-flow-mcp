{
  "fqcn": "graphiant.naas.graphiant_site_to_site_vpn",
  "shortDescription": "Manage Graphiant Site-to-Site VPN configuration",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/graphiant/naas/graphiant_site_to_site_vpn_module.html",
  "options": [
    {
      "name": "access_token",
      "displayName": "Access Token",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Bearer token for API authentication (for example, from C(graphiant login), which opens a browser for sign-in (SSO or non-SSO) and retrieves the token). If not passed as a module argument, the collection reads C(GRAPHIANT_ACCESS_TOKEN) (set after C(graphiant login) when you C(source ~/.graphiant/env.sh)). When a bearer token is present (module argument or environment), it takes precedence over O(username) and O(password). If no valid token is available, the module authenticates with O(username) a",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "detailed_logs",
      "displayName": "Detailed Logs",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Enable detailed logging output for troubleshooting and monitoring. When enabled, provides comprehensive logs of all Site-to-Site VPN operations. Logs are captured and included in the result_msg for display using M(ansible.builtin.debug) module.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "host",
      "displayName": "Host",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Graphiant portal host URL for API connectivity. Example: \"https://api.graphiant.com\"",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "operation",
      "displayName": "Operation",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The specific Site-to-Site VPN operation to perform. V(create): Create Site-to-Site VPN connections on edge devices. V(delete): Delete Site-to-Site VPN connections from edge devices.",
      "choices": [
        "create",
        "delete"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "password",
      "displayName": "Password",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Graphiant portal password for authentication. Required for password-based login when no valid bearer token is available from O(access_token) or C(GRAPHIANT_ACCESS_TOKEN).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "site_to_site_vpn_config_file",
      "displayName": "Site To Site Vpn Config File",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Path to the Site-to-Site VPN configuration YAML file. Required for all operations. Can be an absolute path or relative path. Relative paths are resolved using the configured config_path. Configuration files support Jinja2 templating syntax for dynamic generation. File must contain Site-to-Site VPN definitions with device names and VPN configurations.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "The desired state of the Site-to-Site VPN configuration. V(present): Maps to V(create) when O(operation) not specified. V(absent): Maps to V(delete) when O(operation) not specified.",
      "choices": [
        "present",
        "absent"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "username",
      "displayName": "Username",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Graphiant portal username for authentication. Required for password-based login when no valid bearer token is available from O(access_token) or C(GRAPHIANT_ACCESS_TOKEN).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "vault_bgp_md5_passwords",
      "displayName": "Vault Bgp Md5 Passwords",
      "type": "dict",
      "required": false,
      "default": {},
      "description": "Dict of VPN name to BGP MD5 password (create only). Pass from playbook vars loaded from encrypted I(vault_secrets.yml); secrets in memory only. Key must match the VPN C(name) in the config. Optional; BGP VPNs without an entry get no md5Password.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "vault_site_to_site_vpn_keys",
      "displayName": "Vault Site To Site Vpn Keys",
      "type": "dict",
      "required": false,
      "default": {},
      "description": "Dict of VPN name to preshared key (create only). Pass from playbook vars loaded from encrypted I(vault_secrets.yml); secrets in memory only. Key must match the VPN C(name) in the S2S config. Required for create when the config defines VPNs that need a preshared key.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
