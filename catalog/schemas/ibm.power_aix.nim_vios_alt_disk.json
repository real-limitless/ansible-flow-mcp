{
  "fqcn": "ibm.power_aix.nim_vios_alt_disk",
  "shortDescription": "Uses NIM to create/cleanup an alternate rootvg disk of VIOS clients.",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/ibm/power_aix/nim_vios_alt_disk_module.html",
  "options": [
    {
      "name": "action",
      "displayName": "Action",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Specifies the operation to perform on the VIOS. Specifies the action to perform. C(alt_disk_copy) to perform and alternate disk copy. C(alt_disk_clean) to cleanup an existing alternate disk copy.",
      "choices": [
        "alt_disk_copy",
        "alt_disk_clean"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "disk_size_policy",
      "displayName": "Disk Size Policy",
      "type": "string",
      "required": false,
      "default": "nearest",
      "description": "Specifies how to choose the alternate disk when not specified. C(minimize) smallest disk that can be selected. C(upper) first disk found bigger than the rootvg disk. C(lower) disk size less than rootvg disk size but big enough to contain the used PPs. C(nearest) disk size closest to the rootvg disk.",
      "choices": [
        "minimize",
        "upper",
        "lower",
        "nearest"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "force",
      "displayName": "Force",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Forces removal of any existing alternate disk copy on target disks. Stops any active rootvg mirroring during the alternate disk copy.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "nim_node",
      "displayName": "Nim Node",
      "type": "dict",
      "required": false,
      "default": null,
      "description": "Allows to pass along NIM node info from a previous task to another so that it discovers NIM info only one time for all tasks. The current task might update the NIM info it needs.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "targets",
      "displayName": "Targets",
      "type": "list",
      "required": true,
      "default": null,
      "description": "Specifies the NIM VIOS clients to perform the action on. Uses a dictionary format, with the key being the VIOS NIM name and the value being the list of disks used for the alternate disk copy. When no target disks is specified, the alternate disk copy is performed to only one alternate disk even if the rootvg contains multiple disks.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "time_limit",
      "displayName": "Time Limit",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Before starting the action, the actual date is compared to this parameter value; if it is greater then the task is stopped. The valid format is C(mm/dd/yyyy hh:mm).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "vios_status",
      "displayName": "Vios Status",
      "type": "dict",
      "required": false,
      "default": null,
      "description": "Specifies the result of a previous operation. If set then the I(vios_status) of a target tuple must contain C(SUCCESS) to attempt update. If no I(vios_status) value is found for a tuple, then returned I(status) for this tuple is set to C(SKIPPED-NO-PREV-STATUS).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
