{
  "fqcn": "bodsch.core.syslog_cmd",
  "shortDescription": "Run syslog-ng with arbitrary command-line parameters.",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/bodsch/core/syslog_cmd_module.html",
  "options": [
    {
      "name": "parameters",
      "displayName": "Parameters",
      "type": "list",
      "required": true,
      "default": null,
      "description": "List of command-line parameters passed to C(syslog-ng). Each item may contain a single parameter or a parameter together with its value. Items containing whitespace are split into multiple arguments before execution.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
