{
  "fqcn": "f5networks.f5_bigip.bigiq_as3_deploy",
  "shortDescription": "Manages AS3 declarations sent to BIG-IQ",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/f5networks/f5_bigip/bigiq_as3_deploy_module.html",
  "options": [
    {
      "name": "bigip_device",
      "displayName": "Bigip Device",
      "type": "string",
      "required": false,
      "default": null,
      "description": "The BIG-IP device on which to remove the declaration. Parameter required when C(state) is absent, it is otherwise ignored.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "content",
      "displayName": "Content",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Declaration to be configured on the system. This parameter is most often used along with the C(file) or C(template) lookup plugins. Refer to the examples section for correct usage. For anything advanced or with formatting consider using the C(template) lookup. This can additionally be used for specifying application service configurations directly in YAML, however that is not an encouraged practice and, if used at all, should only be used for the absolute smallest of configurations to prevent yo",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "When C(state) is C(present), ensures the declaration is exists. When C(state) is C(absent), ensures that the declaration is removed.",
      "choices": [
        "present",
        "absent"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "tenant",
      "displayName": "Tenant",
      "type": "string",
      "required": false,
      "default": null,
      "description": "An AS3 tenant you want to remove. Parameter required when C(state) is absent, it is otherwise ignored.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "timeout",
      "displayName": "Timeout",
      "type": "number",
      "required": false,
      "default": 300,
      "description": "The amount of time in seconds to wait for the AS3 async interface to complete its task. The accepted value range is between C(10) and C(1800) seconds.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
