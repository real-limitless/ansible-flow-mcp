{
  "fqcn": "community.libvirt.virt_secret",
  "shortDescription": "Manage libvirt secrets and their values",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/community/libvirt/virt_secret_module.html",
  "options": [
    {
      "name": "command",
      "displayName": "Command",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Executes commands to manage secret. If defined V(create), creates secret described by either O(xml)\nor(secret). It is analagous to O(state=present).\n If defined V(delete), removes secret defined by either O(uuid),\nO(state), O(xml). It is analagous to O(state=absent).\n If defined V(list_secrets) it will return RV(list_secrets) with all\nexisting secrets in XML format.\n If defined V(get_xml) module will try to get secret definition from\nlibvirt. If existing secret found, returns RV(secret_xml). Oth",
      "choices": [
        "create",
        "delete",
        "list_secrets",
        "get_xml",
        "set_value"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "password",
      "displayName": "Password",
      "type": "string",
      "required": false,
      "default": null,
      "description": "A value of the secret which will be stored in the libvirt secret. As in majority cases secrets are private, password is defined only\nduring secret creation or when O(command=set_value) is executed. If the\nsecret already exist and the O(password) is defined, it will not try to\nset it once again. Even if different password is provided.\n",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "secret",
      "displayName": "Secret",
      "type": "dict",
      "required": false,
      "default": null,
      "description": "Defines a secret as a set of fields instead of raw XML. This property is mutually exclusive with O(xml). If O(uuid) is not defined, O(secret.usage) and O(secret.usage_id) must be defined.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Alternative for the O(command) option. If V(present), creates a volume described by either O(xml) or O(secret). Can be used to update existing secrets properties. If V(absent), removes secret defined by either O(uuid), O(state) or O(xml). If V(present) and UUID is not specified in either O(uuid) or O(xml), it\nwill try to find existing secret using O(secret.usage) and\nO(secret.usage_id)\n Mutually exclusive with O(command).",
      "choices": [
        "present",
        "absent"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "uri",
      "displayName": "Uri",
      "type": "string",
      "required": false,
      "default": "qemu:///system",
      "description": "Libvirt connection uri.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "uuid",
      "displayName": "Uuid",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Secret UUID. The value is unique across all secret types. If UUID value is also defined in the O(xml) this option is ignored.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "xml",
      "displayName": "Xml",
      "type": "string",
      "required": false,
      "default": null,
      "description": "XML document used with the define command. Must be raw XML content using C(lookup). XML cannot be reference to a file.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
