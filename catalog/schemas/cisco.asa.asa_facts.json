{
  "fqcn": "cisco.asa.asa_facts",
  "shortDescription": "Collect facts from remote devices running Cisco ASA",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/cisco/asa/asa_facts_module.html",
  "options": [
    {
      "name": "context",
      "displayName": "Context",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Specifies which context to target if you are running in the ASA in multiple context mode. Defaults to the current context you login to.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "gather_network_resources",
      "displayName": "Gather Network Resources",
      "type": "list",
      "required": false,
      "default": null,
      "description": "When supplied, this argument will restrict the facts collected to a given subset. Possible values for this argument include all and the resources like interfaces, vlans etc. Can specify a list of values to include a larger subset. Values can also be used with an initial C(!) to specify that a specific subset should not be collected. Values can also be used with an initial C(!) to specify that a specific subset should not be collected. Valid subsets are 'all', 'acls', 'ogs'.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "gather_subset",
      "displayName": "Gather Subset",
      "type": "list",
      "required": false,
      "default": "!config",
      "description": "When supplied, this argument restricts the facts collected to a given subset. Possible values for this argument include C(all), C(min), C(hardware), C(config). Specify a list of values to include a larger subset. Use a value with an initial C(!) to collect all facts except that subset.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "passwords",
      "displayName": "Passwords",
      "type": "boolean",
      "required": false,
      "default": null,
      "description": "Saves running-config passwords in clear-text when set to True. Defaults to False",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
