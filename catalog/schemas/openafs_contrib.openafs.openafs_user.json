{
  "fqcn": "openafs_contrib.openafs.openafs_user",
  "shortDescription": "Create an OpenAFS user",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/openafs_contrib/openafs/openafs_user_module.html",
  "options": [
    {
      "name": "auth_keytab",
      "displayName": "Auth Keytab",
      "type": "string",
      "required": false,
      "default": "admin.keytab",
      "description": "The path on the remote host to the keytab file to be used to authenticate. The keytab file must already be present on the remote host. This option may only be used if a client is installed on the remote node.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "auth_user",
      "displayName": "Auth User",
      "type": "string",
      "required": false,
      "default": "admin",
      "description": "The afs user name to be used when C(localauth) is False. The user must be a member of the C(system:administrators) group and must be a server superuser, that is, set in the C(UserList) file on each server in the cell. Old kerberos 4 '.' separators are automatically converted to modern '/' separators. This option may only be used if a client is installed on the remote node.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "groups",
      "displayName": "Groups",
      "type": "list",
      "required": false,
      "default": null,
      "description": "The OpenAFS group names the user is a member. Non-system groups will be created.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "id",
      "displayName": "Id",
      "type": "number",
      "required": false,
      "default": 0,
      "description": "The OpenAFS pts id. The next available id will be selected if omitted or 0.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "localauth",
      "displayName": "Localauth",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Indicates if the C(-localauth) option is to be used for authentication. This option should only be used when running on a server.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "C(present) create user and groups when not present C(absent) remove user when not present",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "user",
      "displayName": "User",
      "type": "string",
      "required": true,
      "default": null,
      "description": "The OpenAFS username.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
