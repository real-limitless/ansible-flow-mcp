{
  "fqcn": "bodsch.systemd.networkd_profiles",
  "shortDescription": "Manage a set of systemd-networkd profile files in one call.",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/bodsch/systemd/networkd_profiles_module.html",
  "options": [
    {
      "name": "directory",
      "displayName": "Directory",
      "type": "string",
      "required": false,
      "default": "/etc/systemd/network",
      "description": "Target directory where profile files are written.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "mode",
      "displayName": "Mode",
      "type": "string",
      "required": false,
      "default": "0644",
      "description": "File mode of the resulting profile files (octal string).",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "profiles",
      "displayName": "Profiles",
      "type": "dict",
      "required": true,
      "default": null,
      "description": "Mapping of profile categories to profile definitions. Top-level keys C(link), C(netdev), C(network) are optional; unknown keys are ignored. Each category maps profile basenames to a definition dict with the keys C(state) (C(present)/C(absent), default C(present)), C(description) (optional comment block) and C(config) (dict of sections with options). A section value may be a dict (single occurrence) or a list of dicts (multiple occurrences, e.g. several C([Address]) blocks). List values inside a ",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "purge",
      "displayName": "Purge",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Remove profile files in C(directory) that are not managed by this invocation. Only files with the managed extensions (C(.link), C(.netdev), C(.network)) are considered.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "validate",
      "displayName": "Validate",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "If C(true), run ``networkctl reload`` after writing and roll back any profile that appears in the resulting error output. A second reload is performed with the cleaned-up set. Has no effect in check mode.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "validate_strict",
      "displayName": "Validate Strict",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "Only relevant when ``validate=true``. When the environment cannot run ``networkctl reload`` at all (e.g. inside a container without D-Bus, or when ``systemd-networkd.service`` is not active), validation is normally skipped with a warning so that container-based tests (Molecule, Docker) do not fail spuriously. Set this to ``true`` to fail the task in that situation instead.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "validate_timeout",
      "displayName": "Validate Timeout",
      "type": "number",
      "required": false,
      "default": 30,
      "description": "Timeout (seconds) for the ``networkctl reload`` invocation.",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
