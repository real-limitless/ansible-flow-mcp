{
  "fqcn": "maxhoesel.caddy.caddy_config",
  "shortDescription": "Push configuration to a given path in the caddy config",
  "docUrl": "https://docs.ansible.com/ansible/latest/collections/maxhoesel/caddy/caddy_config_module.html",
  "options": [
    {
      "name": "append",
      "displayName": "Append",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "If set and I(path) points to an existing array or array index, the module will tell Caddy to append/insert to the array (using the APIs C(POST/PUT) method), instead of replacing the array/array index (API C(PATCH) method). See the L(Caddy API Documentation, https://caddyserver.com/docs/api\\#patch-configpath) for details.\n",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "caddy_host",
      "displayName": "Caddy Host",
      "type": "string",
      "required": false,
      "default": "http://localhost:2019",
      "description": "Address of the caddy API endpoint",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "content",
      "displayName": "Content",
      "type": "string",
      "required": false,
      "default": null,
      "description": "Content to push to the specified I(path). Must be a dict or list corresponding to the API JSON format. Required if I(state=present).\n",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "create_path",
      "displayName": "Create Path",
      "type": "boolean",
      "required": false,
      "default": true,
      "description": "Whether to create the path pointing to the configuration if it doesn't exist yet. For example, if I(path=apps/http/servers/myservice) and C(apps/http/servers) does not exist yet, the required path entries will be created. Note that any digit path segments are treated as array indices.\n",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "force",
      "displayName": "Force",
      "type": "boolean",
      "required": false,
      "default": false,
      "description": "By default, this module only pushes configurations if changes have been made compared to the currently running config. Set I(force=True) if you always want to push the configuration, even if no changes will be made. Settings this will cause the module to always return C(changed=True)\n",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "path",
      "displayName": "Path",
      "type": "string",
      "required": true,
      "default": null,
      "description": "Configuration path to which the configuration content will be pushed. Note that the path if is automatically prefixed with C(config/). Example: C(apps/http/servers/myservice)\n",
      "choices": null,
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "state",
      "displayName": "State",
      "type": "string",
      "required": false,
      "default": "present",
      "description": "If C(present), the configuration content at I(path) will be created or updated. If C(absent), any existing configuration at I(path) will be removed.\n",
      "choices": [
        "present",
        "absent"
      ],
      "noLog": false,
      "suboptions": null
    },
    {
      "name": "timeout",
      "displayName": "Timeout",
      "type": "number",
      "required": false,
      "default": 30,
      "description": "Timeout for connections to the caddy API in seconds",
      "choices": null,
      "noLog": false,
      "suboptions": null
    }
  ],
  "source": "ansible-doc"
}
